[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chess-move-trainer-experiments"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["python-chess>=1.11,<2"]

[tool.ruff]
line-length = 100
target-version = "py312"
