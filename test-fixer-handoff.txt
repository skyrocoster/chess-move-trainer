CURRENT FAILURE

frontend/src/features/design-system/CalendarDate.test.tsx hangs when a test opens the Base UI popover.

Exact isolated command:

../node_modules/.bin/vitest.cmd --run --project=unit src/features/design-system/CalendarDate.test.tsx -t "opens the Base UI Popover and restores focus when closed" --testTimeout=10000 --hookTimeout=10000

Output stops at:

RUN  v4.1.5 G:/ChessMoveTrainer/frontend

The JavaScript event loop appears blocked, so Vitest's 10-second test timeout never fires. The tool terminates the process at 40-60 seconds.

WHAT WAS TRIED

- Rendering CalendarDate closed works.
- The hang starts when its Popover.Positioner mounts.
- Removing modal does not help.
- Fake timers are not the cause.
- Supplying non-zero mocked element geometry does not help.
- disableAnchorTracking, fixed positioning, virtual anchors, and disabling collision avoidance did not resolve the problem.
- Replacing Popover.Positioner temporarily made the hang disappear, but Popover.Popup then correctly failed because Base UI requires its Positioner context. That diagnostic change was reverted.
- A --pool=forks experiment was started but aborted before producing a result.
- All temporary product-code experiments in CalendarDate.tsx and PromotionPicker.tsx were reverted.

RELATED FAILURE ALREADY REPAIRED

RepertoireBuilderWorkspaceWorkflow.test.tsx had the same problem through its real CalendarDate and PromotionPicker children.

That workspace-level test now mocks those two primitives, following the existing pattern in RepertoireBuilderWorkspace.test.tsx. Its focused result is:

Test Files  1 passed
Tests       17 passed
Duration    6.84s

This edit affects only the test file, not product behavior.

CURRENT ASSESSMENT

The issue is not the calendar's date logic. Base UI 1.7.0's floating Popover.Positioner is failing to settle under this repository's jsdom/Vitest environment. The same behavior occurs in the dedicated PromotionPicker popover tests.

Evidence:

- Ordinary component tests pass.
- Workspace tests pass when these primitives are mocked.
- Drawer/dialog paths pass.
- Actual floating popover paths block the event loop.

WHAT NEEDS TO HAPPEN

1. Preferred: identify and repair the jsdom environment incompatibility so Base UI positioning can run normally.
2. If Base UI positioning cannot reliably run in jsdom, mock only the floating-positioning boundary in unit tests and retain actual popover validation in the existing browser/Storybook layer.
3. Do not remove Popover.Positioner or change production calendar behavior merely to satisfy jsdom.

The remaining frontend test files have not all been completed because execution stopped at CalendarDate.test.tsx.
